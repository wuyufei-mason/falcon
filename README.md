# falcon

一个轻量的网页番茄计时器（Pomodoro Timer）。

## 功能

- 25 分钟专注 / 5 分钟休息的默认流程
- 支持自定义：专注时长、短休息、长休息、每轮专注次数
- 开始 / 暂停 / 重置 / 跳过阶段
- 自动在专注与休息阶段之间切换

## 运行方式

这是纯前端页面，无需安装依赖。直接打开 `index.html` 即可使用。

如果你想本地启动一个静态服务：

```bash
python3 -m http.server 4173
```

然后访问 <http://localhost:4173>。
